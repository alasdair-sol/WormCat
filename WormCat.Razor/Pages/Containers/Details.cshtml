@page
@model WormCat.Razor.Pages.Containers.DetailsModel

@{
    ViewData["Title"] = Model.Container.Name;
}

<h1>@Model.Container.Name</h1>

<div>
    <hr />
    <h6 class="card-text">@Model.Container.UserId</h6>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Container.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Container.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Container.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
